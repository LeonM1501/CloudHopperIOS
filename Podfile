platform :ios, '12.0'

# Minimal Podfile to satisfy CI/CocoaPods checks
project 'Unity-iPhone.xcodeproj'

target 'Unity-iPhone' do
  # Add pods here if needed, for example:
  # pod 'Firebase/Analytics'
end
